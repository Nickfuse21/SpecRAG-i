.env
venv/
.venv/
__pycache__/
*.py[cod]
.pytest_cache/
.ipynb_checkpoints/

# data — large, regenerable, never commit
data/raw/*
data/docx/*
data/parsed/*
data/chunks/*
data/index/*
!data/**/.gitkeep
!data/raw/manifest.tsv

# model caches
.cache/
*.onnx

.vscode/
.idea/
.DS_Store

# eval run artifacts that depend on a live API — regenerate, do not trust a stale copy
eval/answer_details.json

# regenerated on every eval run; the authoritative table lives in README.md
eval/results.json
eval/results.md

# local working notes — kept out of the published tree
CLAUDE.md
docs/
